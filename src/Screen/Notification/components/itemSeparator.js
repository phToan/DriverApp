import { View } from 'react-native';

export const ItemSeparator = () => (
    <View
        style={{
            height: 0.5,
            width: '100%',
            backgroundColor: '#c8c8c8',
            marginLeft: 60,
        }}
    />
);
