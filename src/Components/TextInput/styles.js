import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    label: {
        fontSize: 16,
        fontWeight: '500',
        color: 'black',
        marginBottom: 10,
        marginTop: 15
    },
    star: {
        color: 'red',
        fontSize: 16
    },
    border: {
        borderWidth: 0.6,
        borderRadius: 10,
        paddingVertical: 15,
        paddingHorizontal: 10
    },
    textInput: {
        width: '100%'
    }
})