import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    bt_ok: {
        borderRadius: 40,
        paddingVertical: 15,
        marginHorizontal: 20
    },
    bt_title: {
        fontSize: 18,
        color: "white",
        textAlign: 'center',
        fontWeight: 'bold'
    }
})